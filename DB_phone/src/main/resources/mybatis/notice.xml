<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="notice">
	<insert id="create" parameterType="NoticeDTO">
		insert into notice(no_title, no_content, no_name, no_date, grpno)
		values(#{no_title},#{no_content},#{no_name}, now(), (select ifnull(max(grpno),0)+1 from notice n));
	</insert>
	
	<select id="read" parameterType="int" resultType="NoticeDTO">
		select no_num, no_title, no_content, no_name, no_date, viewcnt
		from notice
		where no_num = #{no_num}
	</select>
	
	<update id="update" parameterType="NoticeDTO">
		update notice
		set no_title=#{no_title}, no_content=#{no_content}
		where no_num = #{no_num}
	</update>
	
	<delete id="delete" parameterType="int">
		delete from notice
		where no_num = #{no_num}
	</delete>
	
	<select id="list" resultType="NoticeDTO" parameterType="Map">
		select no_num, no_title, no_name, no_date, viewcnt
		from notice
			<where>
				<choose>
					<when test="col=='title'">
						title like '%'||#{word}||'%'
					</when>
					<when test="col=='content'">
						content like '%'||#{word}||'%'
					</when>
				</choose>
			</where>
		order by grpno desc, ansnum asc
		limit #{sno}, #{eno}
	</select>
	
	<update id="upViewcnt" parameterType="int">
		update notice
		set viewcnt = viewcnt + 1
		where no_num = #{no_num}
	</update>
	
	<select id="total" resultType="int" parameterType="Map">
		select count(*) from notice
		<where>
			<choose>
				<when test="col=='title'">
					title like '%'||#{word}||'%'
				</when>
				<when test="col=='content'">
					content like '%'||#{word}||'%'
				</when>
			</choose>
		</where>
	</select>
</mapper>